## Tasks

- [ ] Criar router `POST /v1/equation/solve` no FastAPI
- [ ] Criar schema Pydantic para o body: `equation: str`, `showSteps: bool`
- [ ] Criar schema Pydantic para a resposta: `result: str`, `steps: list[Step]`
- [ ] Implementar parser da expressão (validar se é uma equação válida)
- [ ] Implementar solver linear (ex: `2*x + 5 = 15`)
- [ ] Implementar solver quadrático (ex: `x^2 + 3x + 2 = 0`)
- [ ] Implementar solver de sistemas (ex: `x + y = 3, x - y = 1`)
- [ ] Mapear exceções para HTTP 400 (equação inválida) e 422 (tipo não suportado)
- [ ] Implementar cancelamento via `request.is_disconnected()` antes de processar
- [ ] Escrever testes unitários dos solvers

---------
## Tasks

- [ ] Criar módulo `equation_type_detector.py`
- [ ] Implementar detecção de equação linear (grau 1)
- [ ] Implementar detecção de equação quadrática (grau 2)
- [ ] Implementar detecção de sistema de equações (múltiplas equações separadas por vírgula ou quebra de linha)
- [ ] Implementar função de dispatch: recebe tipo → chama solver correto
- [ ] Escrever testes unitários para os 3 tipos + 1 caso inválido
--------------
## Tasks

- [ ] Criar conexão async com MongoDB usando `motor`
- [ ] Criar model/schema da collection `EquationHistory`
- [ ] Criar índice único composto `(username, equation, createdAt)` no MongoDB
- [ ] Implementar função `save_equation_history()` com upsert
- [ ] Integrar verificação de JWT no endpoint (consumir módulo do colega de auth)
- [ ] Chamar `save_equation_history()` de forma assíncrona (fire-and-forget) após resolver
- [ ] Implementar logging de erro caso a persistência falhe (sem propagar ao cliente)
- [ ] Escrever teste: usuário logado → verifica que persistência foi chamada
- [ ] Escrever teste: usuário não logado → verifica que persistência não foi chamada

--------------
## Tasks

- [ ] Criar router `GET /v1/equation/history` no FastAPI
- [ ] Implementar middleware/dependency de validação JWT (reutilizar módulo do colega)
- [ ] Implementar query no MongoDB filtrando por `username` do JWT, ordenado por `createdAt` DESC
- [ ] Implementar paginação com `?page=1&limit=3` usando `skip` e `limit`
- [ ] Retornar header `X-Total-Count` com total de registros do usuário
- [ ] Checar `request.is_disconnected()` antes de executar a query
- [ ] Escrever teste: JWT válido + histórico existente → retorna lista paginada corretamente
- [ ] Escrever teste: sem JWT → 401
- [ ] Escrever teste: JWT válido + sem histórico → retorna array vazio, não erro
-------------------
## Tasks

- [ ] Inicializar projeto com `poetry` e criar `pyproject.toml` //Matheus já fez
- [ ] Instalar dependências base: `fastapi`, `uvicorn`, `motor`, `sympy`, `pydantic`, `python-jose`
- [ ] Criar estrutura de pastas: `api/`, `solvers/`, `services/`, `db/`, `core/`, `tests/`
- [ ] Configurar variáveis de ambiente com `pydantic-settings` e criar `.env.example`
- [ ] Configurar linter `ruff` com `pyproject.toml`
- [ ] Criar `Makefile` com targets: `run`, `test`, `lint`
- [ ] Escrever `README.md` com passo a passo de setup loca
-------------------------
## Tasks mais tarde

- [ ] Configurar `pytest` com `httpx` e `TestClient` do FastAPI
- [ ] Configurar mock do MongoDB com `mongomock-motor` ou `pytest-mock`
- [ ] Escrever fixture reutilizável de JWT válido para os testes
- [ ] Escrever testes de integração do `POST /v1/equation/solve` (5 cenários da US 1.1)
- [ ] Escrever testes de integração do `GET /v1/equation/history` (3 cenários da US 2.1)
- [ ] Configurar relatório de cobertura com `pytest-cov`
- [ ] Garantir cobertura mínima de 80% (quebrar o build se não atingir)